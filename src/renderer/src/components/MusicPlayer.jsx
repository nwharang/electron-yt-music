const MusicPlayer = ({ PlayingSong }) => {
  return (
    <div className="flex-1 h-[calc(100%-5rem)] overflow-y-auto dark:bg-slate-900 bg-gray-300">
      <div className="relative h-full flex-col flex-1 flex items-center justify-between">Hello</div>
    </div>
  )
}

export default MusicPlayer
